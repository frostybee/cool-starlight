---
// SlideViewer.astro - A component that provides slide presentation functionality
---

<div id="slide-viewer-trigger">
  <button id="start-slideshow" class="slide-viewer-button">
    📽️ View as Slides
  </button>
</div>

<div id="slide-viewer-modal" class="slide-modal hidden">
  <div class="slide-container">
    <div class="slide-toc-sidebar" id="slide-toc-sidebar">
      <div class="slide-toc-header">
        <h3>Table of Contents</h3>
        <button id="toggle-toc" class="toc-toggle-btn" aria-label="Toggle table of contents">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M19 12H5m0 0l7 7m-7-7l7-7"/>
          </svg>
        </button>
      </div>
      <div class="slide-toc-content" id="slide-toc-content">
        <!-- TOC items will be inserted here -->
      </div>
    </div>

    <div class="slide-main-area">
      <div class="slide-content-wrapper">
        <div id="slide-content" class="slide-content">
          <!-- Slides will be inserted here -->
        </div>
        <div id="slide-number-indicator" class="slide-number-indicator">
          1
        </div>
      </div>
      
      <div class="slide-navigation">
        <button id="prev-slide" class="slide-nav-btn" aria-label="Previous slide">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M19 12H5m0 0l7 7m-7-7l7-7"/>
          </svg>
          Previous
        </button>
        <div class="slide-counter" id="slide-counter" title="Click to go to Slide:">
          <input type="number" id="slide-input" class="slide-input" min="1" max="999" value="1" readonly />
          <span class="slide-separator"> / </span>
          <span id="total-slides">1</span>
        </div>
        <div class="slide-font-adjuster">
          <button id="slide-decrease-font" class="font-adj-btn" aria-label="Decrease font size" title="Decrease font size">A-</button>
          <button id="slide-reset-font" class="font-adj-btn" aria-label="Reset font size" title="Reset font size">A</button>
          <button id="slide-increase-font" class="font-adj-btn" aria-label="Increase font size" title="Increase font size">A+</button>
        </div>
        <button id="toggle-fullscreen" class="slide-nav-btn fullscreen-btn" aria-label="Toggle fullscreen">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M8 3H5a2 2 0 0 0-2 2v3m18 0V5a2 2 0 0 0-2-2h-3m0 18h3a2 2 0 0 0 2-2v-3M3 16v3a2 2 0 0 0 2 2h3"/>
          </svg>
        </button>
        <button id="close-slideshow" class="slide-nav-btn close-btn" aria-label="Close slideshow">
          ✕
        </button>
        <button id="next-slide" class="slide-nav-btn" aria-label="Next slide">
          Next
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M5 12h14m0 0l-7-7m7 7l-7 7"/>
          </svg>
        </button>
      </div>
    </div>
  </div>
  
  <!-- Go to slide modal -->
  <div id="goto-slide-modal" class="goto-modal hidden">
    <div class="goto-modal-content">
      <h3>Go to Slide:</h3>
      <input type="number" id="goto-slide-input" class="goto-input" min="1" max="1" placeholder="Enter slide number" />
      <div class="goto-buttons">
        <button id="goto-confirm" class="goto-btn goto-confirm">Go</button>
        <button id="goto-cancel" class="goto-btn goto-cancel">Cancel</button>
      </div>
    </div>
  </div>
</div>

<style>
  :root {
    /* TOC Color Variables for Light Theme */
    --toc-bg: #f8fafc;
    --toc-border: #e2e8f0;
    --toc-text: var(--sl-color-text);
    --toc-hover-bg: #e0f2fe;
    --toc-hover-border: #3b82f6;
    --toc-hover-shadow: rgba(59, 130, 246, 0.2);
    --toc-active-bg: #fbbf24;
    --toc-active-text: #1f2937;
    --toc-active-border: #f59e0b;
    --toc-active-shadow: rgba(251, 191, 36, 0.4);
    --toc-number-bg: var(--sl-color-accent);
    --toc-number-text: var(--sl-color-accent-text);
    --toc-number-shadow: rgba(99, 102, 241, 0.3);
  }

  /* Dark theme overrides - using multiple selectors for Starlight compatibility */
  [data-theme="dark"],
  :root[data-theme="dark"],
  html[data-theme="dark"],
  .sl-theme-dark {
    --toc-bg: #1e293b;
    --toc-border: #374151;
    --toc-text: #e2e8f0;
    --toc-hover-bg: #334155;
    --toc-hover-border: #3b82f6;
    --toc-hover-shadow: rgba(59, 130, 246, 0.3);
    --toc-active-bg: #fbbf24;
    --toc-active-text: #1f2937;
    --toc-active-border: #f59e0b;
    --toc-active-shadow: rgba(251, 191, 36, 0.4);
    --toc-number-bg: var(--sl-color-accent);
    --toc-number-text: var(--sl-color-accent-text);
    --toc-number-shadow: rgba(79, 70, 229, 0.4);
    --slide-text-color: #ffffff;
    --slide-heading-color: #ffffff;
  }

  /* Additional dark theme support for common dark mode patterns */
  @media (prefers-color-scheme: dark) {
    :root:not([data-theme="light"]) {
      --toc-bg: #1e293b;
      --toc-border: #374151;
      --toc-text: #e2e8f0;
      --toc-hover-bg: #334155;
      --toc-hover-border: #3b82f6;
      --toc-hover-shadow: rgba(59, 130, 246, 0.3);
      --toc-active-bg: #fbbf24;
      --toc-active-text: #1f2937;
      --toc-active-border: #f59e0b;
      --toc-active-shadow: rgba(251, 191, 36, 0.4);
      --toc-number-bg: var(--sl-color-accent);
      --toc-number-text: var(--sl-color-accent-text);
      --toc-number-shadow: rgba(79, 70, 229, 0.4);
      --slide-text-color: #ffffff;
      --slide-heading-color: #ffffff;
    }
  }

  @layer slide-toc-overrides {
    .toc-item {
      background: var(--toc-bg);
      border: 2px solid var(--toc-border);
      border-radius: 8px;
      margin: 8px;
      padding: 12px 16px;
      display: flex;
      align-items: center;
      width: calc(100% - 16px);
      cursor: pointer;
      transition: all 0.2s ease;
      color: var(--toc-text);
      text-decoration: none;
      text-align: left;
      font-size: 0.9rem;
      line-height: 1.4;
      font-weight: 500;
    }

    .toc-item:hover {
      background: var(--toc-hover-bg);
      border-color: var(--toc-hover-border);
      transform: translateY(-2px);
      box-shadow: 0 4px 12px var(--toc-hover-shadow);
    }

    .toc-item.active {
      background: var(--toc-active-bg);
      color: var(--toc-active-text);
      border-color: var(--toc-active-border);
      box-shadow: 0 4px 12px var(--toc-active-shadow);
    }

    .toc-item.h3 {
      font-size: 0.85rem;
      margin-left: 1rem;
    }
  }

  .slide-viewer-button {
    background: var(--sl-color-accent);
    color: var(--sl-color-accent-text);
    border: none;
    padding: 0.75rem 1.5rem;
    border-radius: 0.5rem;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s ease;
    margin: 1rem 0;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
  }

  .slide-viewer-button:hover {
    background: var(--sl-color-accent-high);
    transform: translateY(-1px);
  }

  .slide-modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background: linear-gradient(135deg, #1e3c72, #2a5298);
    z-index: 2147483647 !important;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .slide-modal.hidden {
    display: none;
  }

  .slide-container {
    width: 98%;
    max-width: 100vw;
    height: 100vh;
    background: var(--sl-color-bg);
    display: flex;
    flex-direction: row;
    overflow: hidden;
    margin: 0 auto;
    border-radius: 8px;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
  }

  .slide-toc-sidebar {
    width: 300px;
    min-width: 300px;
    height: 100%;
    background: var(--sl-color-bg-sidebar);
    border-right: 1px solid var(--sl-color-gray-5);
    display: flex;
    flex-direction: column;
    transition: all 0.3s ease;
    overflow: hidden;
  }

  .slide-toc-sidebar.collapsed {
    width: 50px;
    min-width: 50px;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .slide-toc-sidebar.collapsed:hover {
    background: var(--sl-color-gray-6);
    box-shadow: 2px 0 8px rgba(0, 0, 0, 0.1);
  }

  .slide-toc-header {
    padding: 1rem;
    border-bottom: 1px solid var(--sl-color-gray-5);
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: var(--sl-color-bg-nav);
    min-height: 60px;
    box-sizing: border-box;
  }

  .slide-toc-header h3 {
    margin: 0;
    font-size: 1rem;
    color: var(--sl-color-text);
    transition: opacity 0.3s ease, visibility 0.3s ease;
    white-space: nowrap;
    overflow: hidden;
  }

  .slide-toc-sidebar.collapsed .slide-toc-header h3 {
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
  }

  .slide-toc-sidebar.collapsed .slide-toc-header {
    justify-content: center;
    padding: 0.5rem;
  }

  .toc-toggle-btn {
    background: none;
    border: none;
    font-size: 1.2rem;
    cursor: pointer;
    color: var(--sl-color-text);
    padding: 0.25rem;
    border-radius: 4px;
    transition: background-color 0.2s;
    pointer-events: auto;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .toc-toggle-btn:hover {
    background: var(--sl-color-gray-6);
  }

  .slide-toc-sidebar.collapsed .toc-toggle-btn {
    transform: rotate(180deg);
  }

  .slide-toc-content {
    flex: 1;
    overflow-y: auto;
    padding: 0.5rem 0;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    transition: opacity 0.3s ease, visibility 0.3s ease;
  }

  .slide-toc-sidebar.collapsed .slide-toc-content {
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    overflow: hidden;
  }

  .slide-toc-sidebar.collapsed .toc-item-container {
    display: none !important;
  }


  .toc-item-container:has(.toc-item.active) .toc-item-number {
    background: linear-gradient(135deg, #3b82f6, #8b5cf6);
    color: #ffffff;
    box-shadow: 0 4px 15px rgba(59, 130, 246, 0.5);
  }

  .toc-item-number {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
    background: var(--toc-number-bg);
    border-radius: 50%;
    font-size: 0.75rem;
    font-weight: 700;
    color: var(--toc-number-text);
    flex-shrink: 0;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 2px 10px var(--toc-number-shadow);
    border: 2px solid rgba(255, 255, 255, 0.2);
    position: relative;
    overflow: hidden;
  }

  .toc-item-number::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: conic-gradient(from 0deg, transparent, rgba(255, 255, 255, 0.3), transparent);
    animation: rotate 3s linear infinite;
    opacity: 0;
    transition: opacity 0.3s;
  }

  .toc-item-container:hover .toc-item-number {
    background: linear-gradient(135deg, #3b82f6, #8b5cf6);
    transform: scale(1.2) rotate(10deg);
    box-shadow: 0 6px 20px rgba(59, 130, 246, 0.5);
    border-color: rgba(255, 255, 255, 0.4);
  }

  .toc-item-container:hover .toc-item-number::before {
    opacity: 1;
  }

  @keyframes rotate {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  .toc-item-title {
    flex: 1;
    text-align: left;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    transition: all 0.3s ease;
    font-weight: 500;
  }

  .toc-item-container:hover .toc-item-title {
    font-weight: 600;
    text-shadow: 0 2px 8px rgba(59, 130, 246, 0.3);
  }

  .slide-toc-content .toc-item-container.h3 {
    margin-left: 2rem !important;
    background: #f8fafc !important;
    border-color: #cbd5e1 !important;
    box-shadow: 0 2px 12px rgba(0, 0, 0, 0.06) !important;
  }

  .slide-toc-content .toc-item-container.h3 .toc-item {
    font-size: 0.85rem !important;
  }

  .slide-toc-content .toc-item-container.h3:hover {
    background: linear-gradient(135deg, #eef2ff, #e0e7ff) !important;
    transform: translateY(-3px) scale(1.01) !important;
    box-shadow: 0 16px 32px rgba(99, 102, 241, 0.12), 0 6px 20px rgba(0, 0, 0, 0.08) !important;
    border-color: #6366f1 !important;
  }

  .toc-item-container.h3 .toc-item-number {
    width: 24px;
    height: 24px;
    font-size: 0.7rem;
    background: linear-gradient(135deg, #6366f1, #a855f7);
    box-shadow: 0 2px 8px rgba(99, 102, 241, 0.4);
  }

  .toc-item-container.h3:hover .toc-item-number {
    transform: scale(1.15) rotate(8deg);
    box-shadow: 0 4px 15px rgba(99, 102, 241, 0.6);
  }

  .slide-main-area {
    flex: 1;
    display: flex;
    flex-direction: column;
    position: relative;
    min-height: 0;
    min-width: 0;
  }



  .slide-counter {
    font-size: 2.1rem;
    font-weight: 600;
    color: var(--sl-color-text);
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    padding: 0.5rem 1rem;
    border-radius: 0.5rem;
    cursor: pointer;
    position: relative;
    display: inline-block;
  }

  .slide-counter:hover {
    background: var(--sl-color-gray-6);
  }

  .slide-input {
    display: inline-block;
    width: 6ch;
    background: var(--sl-color-bg);
    border: 2px solid var(--sl-color-accent);
    border-radius: 0.25rem;
    text-align: center;
    font-size: 2.1rem;
    font-weight: 600;
    color: var(--sl-color-text);
    padding: 0.25rem 0.75rem;
    margin: 0;
    vertical-align: baseline;
  }
  
  .slide-input[readonly] {
    cursor: pointer;
    border-color: var(--sl-color-gray-4);
  }
  
  .slide-input[readonly]:hover {
    border-color: var(--sl-color-accent);
  }

  .slide-input:focus {
    outline: none;
    border-color: var(--sl-color-accent-high);
    box-shadow: 0 0 0 3px rgba(var(--sl-color-accent), 0.3);
  }

  .slide-counter.animating {
    /* Animation removed for cleaner input experience */
  }

  @keyframes scaleNumber {
    0% {
      transform: scale(1);
      color: var(--sl-color-text);
      background: transparent;
      box-shadow: none;
    }
    50% {
      transform: scale(1.2);
      color: white;
      background: var(--sl-color-accent);
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3), 0 0 20px var(--sl-color-accent-low);
    }
    100% {
      transform: scale(1);
      color: var(--sl-color-text);
      background: transparent;
      box-shadow: none;
    }
  }

  .slide-content-wrapper {
    flex: 1;
    overflow-y: auto;
    scrollbar-width: thin;
    scrollbar-color: var(--sl-color-gray-3) transparent;
    min-height: 0;
  }

  .slide-content-wrapper::-webkit-scrollbar {
    width: 8px;
  }

  .slide-content-wrapper::-webkit-scrollbar-track {
    background: transparent;
  }

  .slide-content-wrapper::-webkit-scrollbar-thumb {
    background: var(--sl-color-gray-3);
    border-radius: 4px;
  }

  .slide-content {
    padding: 4rem;
    min-height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    align-items: stretch;
    text-align: left;
    transition: opacity 0.3s ease, transform 0.3s ease;
    width: 100%;
    margin: 0 auto;
    box-sizing: border-box;
  }

  .slide-content.transitioning {
    opacity: 0;
    transform: translateX(20px);
  }

  .slide-number-indicator {
    position: absolute;
    top: 2rem;
    right: 2rem;
    width: 87px;
    height: 87px;
    background: #1e3a8a;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 3.2rem;
    font-weight: 700;
    color: white;
    box-shadow: 0 4px 20px rgba(30, 58, 138, 0.4);
    border: 3px solid rgba(255, 255, 255, 0.3);
    z-index: 10;
    transition: all 0.3s ease;
  }

  .slide-number-indicator:hover {
    transform: scale(1.1);
    box-shadow: 0 6px 25px rgba(30, 58, 138, 0.6);
  }

  .slide-number-indicator.animating {
    animation: slideNumberScale 0.6s ease-out;
  }

  @keyframes slideNumberScale {
    0% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.3);
    }
    100% {
      transform: scale(1);
    }
  }



  .slide-navigation {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.5rem 2rem;
    background: var(--sl-color-bg-nav);
    border-top: 1px solid var(--sl-color-gray-5);
    flex-shrink: 0;
    z-index: 5;
  }

  .slide-nav-btn {
    background: var(--sl-color-accent);
    color: var(--sl-color-accent-text);
    border: none;
    padding: 0.75rem 1.5rem;
    border-radius: 0.5rem;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s ease;
    min-width: 120px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
  }

  .slide-nav-btn:hover:not(:disabled) {
    background: var(--sl-color-accent-high);
    transform: translateY(-1px);
  }

  .slide-nav-btn:disabled {
    background: var(--sl-color-gray-4);
    color: var(--sl-color-gray-2);
    cursor: not-allowed;
    transform: none;
  }

  /* Different colors for previous and next buttons */
  #prev-slide {
    background: #ef4444;
    color: white;
  }

  #prev-slide:hover:not(:disabled) {
    background: #dc2626;
  }

  #next-slide {
    background:rgb(8, 64, 160);
    color: white;
  }

  #next-slide:hover:not(:disabled) {
    background:rgb(179, 151, 10);
  }

  .close-btn {
    background: #dc2626;
    color: white;
    min-width: auto;
    width: 40px;
    height: 40px;
    border-radius: 15%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
    padding: 0;
  }

  .close-btn:hover {
    background: #b91c1c;
  }

  .fullscreen-btn {
    background: #16a34a;
    color: white;
    min-width: auto;
    width: 40px;
    height: 40px;
    border-radius: 15%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
    padding: 0;
  }

  .fullscreen-btn:hover {
    background: #15803d;
  }

  /* Go to slide modal styles */
  .goto-modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.8);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 2147483648;
  }

  .goto-modal.hidden {
    display: none;
  }

  .goto-modal-content {
    background: var(--sl-color-bg);
    border-radius: 0.5rem;
    padding: 2rem;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    border: 1px solid var(--sl-color-gray-5);
    min-width: 300px;
    text-align: center;
  }

  .goto-modal-content h3 {
    margin: 0 0 1rem 0;
    color: var(--sl-color-text);
    font-size: 1.5rem;
  }

  .goto-input {
    width: 100%;
    padding: 0.75rem;
    font-size: 1.2rem;
    text-align: center;
    border: 2px solid var(--sl-color-gray-5);
    border-radius: 0.5rem;
    background: var(--sl-color-bg);
    color: var(--sl-color-text);
    margin-bottom: 1rem;
  }

  .goto-input:focus {
    outline: none;
    border-color: var(--sl-color-accent);
    box-shadow: 0 0 0 3px rgba(var(--sl-color-accent), 0.3);
  }

  .goto-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
  }

  .goto-btn {
    padding: 0.75rem 1.5rem;
    border: none;
    border-radius: 0.5rem;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .goto-confirm {
    background: var(--sl-color-accent);
    color: var(--sl-color-accent-text);
  }

  .goto-confirm:hover {
    background: var(--sl-color-accent-high);
  }

  .goto-cancel {
    background: var(--sl-color-gray-5);
    color: var(--sl-color-text);
  }

  .goto-cancel:hover {
    background: var(--sl-color-gray-4);
  }

  /* Font size adjuster styles */
  .slide-font-adjuster {
    display: flex;
    gap: 1rem;
    align-items: center;
  }

  .font-adj-btn {
    background-color: var(--sl-color-gray-5);
    border: none;
    border-radius: 0.25rem;
    color: var(--sl-color-white);
    cursor: pointer;
    font-weight: bold;
    padding: 0.25rem 0.5rem;
    transition: all 0.2s ease;
    position: relative;
    min-width: 80px;
  }

  .font-adj-btn:hover {
    background-color: var(--sl-color-gray-4);
    transform: scale(1.3);
  }

  .font-adj-btn::after {
    content: attr(title);
    position: absolute;
    bottom: 100%;
    left: 50%;
    transform: translateX(-50%);
    background-color: var(--sl-color-gray-6, #333);
    color: var(--sl-color-white, #fff);
    padding: 0.25rem 0.5rem;
    border-radius: 0.25rem;
    font-size: 0.75rem;
    white-space: nowrap;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.2s, visibility 0.2s;
    pointer-events: none;
    z-index: 10;
    margin-bottom: 0.5rem;
  }

  .font-adj-btn:hover::after {
    opacity: 1;
    visibility: visible;
  }

  #slide-decrease-font {
    font-size: 0.8rem;
  }

  #slide-reset-font {
    font-size: 1rem;
  }

  #slide-increase-font {
    font-size: 1.2rem;
  }

  /* Mobile and tablet responsive */
  @media (max-width: 1024px) {
    .slide-container {
      width: 100vw;
      height: 100vh;
      border-radius: 0;
    }

    .slide-navigation {
      padding: 0.5rem;
      flex-wrap: wrap;
      gap: 0.5rem;
      justify-content: center;
    }

    .slide-content {
      padding: 1.5rem;
    }

    .slide-nav-btn {
      padding: 0.5rem 0.8rem;
      font-size: 0.8rem;
      min-width: 80px;
      flex: 0 0 auto;
    }

    .slide-counter {
      font-size: 1.2rem;
      padding: 0.25rem 0.5rem;
    }

    .slide-input {
      font-size: 1.2rem;
      width: 4ch;
      padding: 0.2rem 0.5rem;
    }

    .slide-font-adjuster {
      display: none;
    }

    .fullscreen-btn {
      display: none;
    }

    .close-btn {
      width: 35px;
      height: 35px;
      font-size: 1rem;
    }

    .slide-number-indicator {
      width: 50px;
      height: 50px;
      font-size: 1.2rem;
      top: 0.5rem;
      right: 0.5rem;
    }

    /* Hide button text on very small screens, keep icons */
    #prev-slide,
    #next-slide {
      font-size: 0;
      min-width: 45px;
      padding: 0.5rem;
    }

    #prev-slide svg,
    #next-slide svg {
      width: 20px;
      height: 20px;
    }
  }

  /* Extra small screens */
  @media (max-width: 480px) {
    .slide-navigation {
      padding: 0.25rem;
      gap: 0.25rem;
    }

    .slide-nav-btn {
      padding: 0.4rem 0.6rem;
      font-size: 0.7rem;
      min-width: 70px;
    }

    .slide-counter {
      font-size: 1rem;
    }

    .slide-input {
      font-size: 1rem;
      width: 3ch;
    }


    .close-btn {
      width: 30px;
      height: 30px;
      font-size: 0.9rem;
    }

    .slide-number-indicator {
      width: 40px;
      height: 40px;
      font-size: 1rem;
    }

  }

  /* Slide content styling - Reveal.js inspired */
  .slide-content {
    --font-scale: 1;
  }

  .slide-content :global(h1) {
    margin: 0 0 1rem 0;
    font-size: 5rem;
    font-weight: 700;
    line-height: 1.2;
    color: var(--slide-heading-color, var(--sl-color-text));
    text-shadow: 0 0 6px rgba(0, 0, 0, 0.2);
  }

  .slide-content :global(h1 a),
  .slide-content :global(h1 a:hover),
  .slide-content :global(h1 a:visited) {
    color: inherit;
    text-decoration: none;
    pointer-events: none;
  }

  .slide-content :global(h2) {
    margin: 0 0 1rem 0;
    font-size: 4rem;
    font-weight: 600;
    line-height: 1.3;
    color: var(--slide-heading-color, var(--sl-color-text));
  }

  .slide-content :global(h2 a),
  .slide-content :global(h2 a:hover),
  .slide-content :global(h2 a:visited) {
    color: inherit;
    text-decoration: none;
    pointer-events: none;
  }

  .slide-content :global(h3) {
    margin: 0 0 1rem 0;
    font-size: 3rem;
    font-weight: 600;
    line-height: 1.3;
    color: var(--slide-heading-color, var(--sl-color-text));
  }

  .slide-content :global(h3 a),
  .slide-content :global(h3 a:hover),
  .slide-content :global(h3 a:visited) {
    color: inherit;
    text-decoration: none;
    pointer-events: none;
  }

  .slide-content :global(h4) {
    margin: 0 0 1rem 0;
    font-size: 2.5rem;
    font-weight: 500;
    line-height: 1.4;
    color: var(--slide-heading-color, var(--sl-color-text));
  }

  .slide-content :global(h4 a),
  .slide-content :global(h4 a:hover),
  .slide-content :global(h4 a:visited) {
    color: inherit;
    text-decoration: none;
    pointer-events: none;
  }

  .slide-content :global(p) {
    font-size: calc(1.8rem * var(--font-scale));
    line-height: 1.6;
    margin: 0 0 1.5rem 0;
    color: var(--slide-text-color, var(--sl-color-text));
    width: 100%;
  }

  .slide-content :global(li) {
    font-size: calc(1.8rem * var(--font-scale));
    line-height: 1.6;
    margin: 0.5rem 0;
    color: var(--slide-text-color, var(--sl-color-text));
  }

  .slide-content :global(ul),
  .slide-content :global(ol) {
    font-size: calc(1.8rem * var(--font-scale));
    text-align: left;
    margin: 1rem 0;
    width: 100%;
  }

  .slide-content :global(ul) {
    list-style-type: none;
    padding-left: 0;
  }

  .slide-content :global(ul li::before) {
    content: "▶";
    color: var(--sl-color-accent);
    font-weight: bold;
    display: inline-block;
    width: 1em;
    margin-right: 0.5em;
  }

  .slide-content :global(strong) {
    color: var(--sl-color-accent);
    font-weight: 700;
  }

  .slide-content :global(em) {
    color: var(--sl-color-accent);
    font-style: italic;
  }

  .slide-content :global(img) {
    max-width: 80%;
    height: auto;
    border-radius: 8px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
    margin: 2rem 0;
  }

  .slide-content :global(pre) {
    font-size: calc(1.6rem * var(--font-scale));
    overflow-x: auto;
    margin: 2rem 0;
    text-align: left;
    width: 100% !important;
    max-width: 100% !important;
    line-height: 1.5;
    box-sizing: border-box;
    padding: 1rem;
  }

  .slide-content :global(div.expressive-code),
  .slide-content :global(.expressive-code),
  .slide-content :global(.astro-code) {
    width: 100% !important;
    max-width: 100% !important;
    margin: 2rem 0;
  }

  .slide-content :global(code) {
    font-size: calc(1.4rem * var(--font-scale));
  }

  .slide-content :global(blockquote) {
    margin: 2rem 0;
    padding: 2rem;
    border-left: 6px solid var(--sl-color-accent);
    background: var(--sl-color-bg-accent);
    border-radius: 0 8px 8px 0;
    font-style: italic;
    font-size: calc(1.6rem * var(--font-scale));
    color: var(--slide-text-color, var(--sl-color-text));
    width: 100%;
    position: relative;
  }

  .slide-content :global(blockquote::before) {
    content: """;
    font-size: 4rem;
    color: var(--sl-color-accent);
    position: absolute;
    top: -10px;
    left: 20px;
    font-family: serif;
  }
</style>

<script src="/src/scripts/SlideViewer.js"></script>